model_reasoning_effort = "high"
notify = ["notify-send", "-a", "Codex", "Task Completed"]

[projects."/home/david/projects/dotfiles"]
trust_level = "trusted"

[tools]
# Enable web search tool (expects a boolean, not a table)
web_search = true
