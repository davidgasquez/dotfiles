# Select the integrated GPU without waking the discrete GPU during device probing.
--render-node-override=/dev/dri/by-path/pci-0000:07:00.0-render
# Enable hardware video encoding for calls and screen sharing.
--enable-features=AcceleratedVideoEncoder
