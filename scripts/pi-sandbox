#!/usr/bin/env bash
set -euo pipefail

work_dir="$(mktemp -d -t pi-session.XXXXXX)"
session_dir="${HOME}/.pi/agent/sessions/--tmp--"

exec ghostty +new-window --working-directory="${work_dir}" -e zsh -ic "pi --session-dir \"${session_dir}\""
